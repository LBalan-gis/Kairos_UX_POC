// Pure graph algorithm — no company data.
import type { Entity, Relation } from '../types/domain';

export function buildProjectionRelations(entities: Entity[], relations: Relation[]): Relation[] {
  return relations;
}
